---
phase: 00-preflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/__root.tsx
autonomous: true
requirements:
  - BLOCKER-03
must_haves:
  truths:
    - "Root HTML title shows 'KDM Ecosystem' not 'Lovable App'"
    - "OG/twitter meta tags reference KDM Ecosystem, not Lovable"
  artifacts:
    - path: "src/routes/__root.tsx"
      provides: "Root document metadata"
      contains: '"KDM Ecosystem"'
  key_links:
    - from: "src/routes/__root.tsx"
      to: "Browser <title> tag"
      via: "head → meta → title"
      pattern: "title.*KDM Ecosystem"
---

<objective>
Fix root metadata that still references "Lovable App" / "Lovable Generated Project".

Purpose: All pages inherit root metadata. Until fixed, every page tab reads "Lovable App" — a placeholder that was never updated. This must be corrected before any other work begins.

Output: Updated `src/routes/__root.tsx` with ecosystem-neutral metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<interfaces>
Current meta block in src/routes/__root.tsx (lines 72-84):
```typescript
head: () => ({
    meta: [
      { charSet: "utf-8" },
      { name: "viewport", content: "width=device-width, initial-scale=1" },
      { title: "Lovable App" },
      { name: "description", content: "Lovable Generated Project" },
      { name: "author", content: "Lovable" },
      { property: "og:title", content: "Lovable App" },
      { property: "og:description", content: "Lovable Generated Project" },
      { property: "og:type", content: "website" },
      { name: "twitter:card", content: "summary" },
      { name: "twitter:site", content: "@Lovable" },
    ],
    links: [
      { rel: "icon", type: "image/png", href: logoUrl },
    ],
}),
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace root metadata placeholders with KDM Ecosystem branding</name>
  <files>src/routes/__root.tsx</files>
  <action>
    Edit `src/routes/__root.tsx` — replace the `head: () ⇒ ({ meta: [...] })` block.

    Changes required (per D-03):
    - `{ title: "Lovable App" }` → `{ title: "KDM Ecosystem" }`
    - `{ name: "description", content: "Lovable Generated Project" }` → `{ name: "description", content: "Open-source CLI ecosystem for Kubernetes and Docker monitoring." }`
    - `{ name: "author", content: "Lovable" }` → REMOVE this line entirely
    - `{ property: "og:title", content: "Lovable App" }` → `{ property: "og:title", content: "KDM Ecosystem" }`
    - `{ property: "og:description", content: "Lovable Generated Project" }` → `{ property: "og:description", content: "Monitor every pod and container from your terminal." }`
    - `{ name: "twitter:site", content: "@Lovable" }` → REMOVE this line entirely
    - Keep `{ name: "twitter:card", content: "summary" }` as-is

    Do NOT change any other part of the file — the layout, shell, error/not-found components, and imports stay exactly as they are.

    Do NOT add emoji.
    Do NOT touch the links array (logoUrl stays).
  </action>
  <verify>
    <automated>cd D:\Desktop\Yuvraj\kdm-website && node -e "const c=require('fs').readFileSync('src/routes/__root.tsx','utf8'); if(!c.includes('KDM Ecosystem')||!c.includes('Open-source CLI ecosystem')){process.exit(1)}; if(c.includes('Lovable App')||c.includes('@Lovable')){process.exit(2)}; console.log('OK')"</automated>
  </verify>
  <done>
    `src/routes/__root.tsx` has zero references to "Lovable" in meta tags.
    Title displays "KDM Ecosystem".
    `npm run build` compiles without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| n/a | No data flow — pure metadata change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | root metadata | accept | Static meta tags, no user input involved |
</threat_model>

<verification>
1. `grep 'Lovable' src/routes/__root.tsx` returns no matches
2. `npm run build` succeeds
3. Running `npm run dev` and inspecting `<title>` shows "KDM Ecosystem"
</verification>

<success_criteria>
- Root `<title>` reads "KDM Ecosystem"
- OG tags reference KDM Ecosystem, not Lovable
- Build succeeds
</success_criteria>
