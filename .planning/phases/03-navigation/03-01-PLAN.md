---
phase: 03-navigation
plan: 01
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/Navbar.tsx
  - src/components/Footer.tsx
autonomous: true
requirements:
  - NAV-01
must_haves:
  truths:
    - "Navbar renders product-appropriate brand name, links, and GitHub star button"
    - "Footer renders product-appropriate brand name and legal links"
    - "Navbar shows product name + Docs link + Features/Commands anchors + GitHub CTA"
    - "Footer shows brand name, copyright year, Privacy, Terms, GitHub links"
    - "Navbar/Footer work correctly under hub (no product context) and product pages"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Product-aware navigation bar"
      contains: "useProduct"
    - path: "src/components/Footer.tsx"
      provides: "Product-aware footer"
      contains: "useProduct"
  key_links:
    - from: "src/components/Navbar.tsx"
      to: "src/context/ProductContext.tsx"
      via: "import useProduct"
    - from: "src/components/Footer.tsx"
      to: "src/context/ProductContext.tsx"
      via: "import useProduct"
---

<objective>
Make Navbar and Footer render product-specific content from ProductContext.

Purpose: Navbar and Footer currently hardcode "kdm" brand name, GitHub URL, and legal links. They must read from the active product's ProductConfig so they render correctly on kdm pages, Docker Guard pages, and the hub page (where there's no active product, using the kdm default).

Output:
- Updated `src/components/Navbar.tsx` — uses useProduct for brand name, GitHub URL, docs link
- Updated `src/components/Footer.tsx` — uses useProduct for brand name, copyright, GitHub link
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/Navbar.tsx
@src/components/Footer.tsx
@src/context/ProductContext.tsx
@src/config/products/types.ts
</context>

<interfaces>
```typescript
// From ProductContext
function useProduct(): ProductConfig;

// From ProductConfig (relevant fields for nav/footer)
interface ProductConfig {
  slug: string;           // "kdm" | "docker-guard"
  displayName: string;    // "KDM" | "Docker Guard"
  brandName: string;      // "kdm" | "dg"
  githubUrl: string;      // Full GitHub URL
  docsPath: string;       // "/kdm/docs" | "/docker-guard/docs"
  copyright: string;      // "© 2026 KDM Labs"
  // ... other fields not needed here
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize Navbar with useProduct</name>
  <files>src/components/Navbar.tsx</files>
  <action>
    Edit `src/components/Navbar.tsx`:

    1. Add import: `import { useProduct } from "@/context/ProductContext";`

    2. Inside the `Navbar` component (at top):
       ```typescript
       const product = useProduct();
       ```

    3. Replace all hardcoded "kdm" references:
       - Line 16: `kdm` link text → `{product.brandName}`
       - Line 22: Docs link `/docs` → `{product.docsPath}` (but keep the Link component with a dynamic `to` prop — note: `@tanstack/react-router` Link requires a static `to` or you must use conditional rendering. The Docs link should render as `<a href={product.docsPath}>Docs</a>` with a plain anchor instead of Link, OR use two `<Link>` components with conditional rendering. Since TanStack Router Links require static paths at compile time, use a plain `<a>` element for the Docs link.)

    4. Replace the GitHub "Star us" button URLs:
       - Both the mobile Sheet and desktop GitHub CTA buttons reference hardcoded `https://github.com/KDM-cli/kdm-cli`
       - Replace both with `{product.githubUrl}`

    5. The import of `Link` may no longer be needed for the Docs link. Remove the `Link` import if no other `<Link>` is used. However, the brand name link still uses `<Link to="/">` — keep that import. Actually, the brand link uses `<Link to="/">` on line 15, and the docs link uses `<Link to="/docs">` on line 22. Replace the docs Link with a plain `<a href={product.docsPath}>`.

    6. Do NOT change the mobile Sheet structure, CSS classes, layout, or any other functionality.
    7. Do NOT remove the hamburger menu or GitHub CTA buttons.
    8. Keep all existing CSS classes, transitions, and styling.

    The hub page (index.tsx) does NOT render Navbar, so the default product (kdm) case only applies when Navbar is used on a page without a product layout context — which shouldn't happen but is handled gracefully.
  </action>
  <verify>
    <automated>cd D:\Desktop\Yuvraj\kdm-website && node -e "const c=require('fs').readFileSync('src/components/Navbar.tsx','utf8'); if(!c.includes('useProduct')){process.exit(1)}; if(!c.includes('product.brandName')){process.exit(2)}; if(!c.includes('product.githubUrl')){process.exit(3)}; if(c.includes('/docs')){process.exit(4)}; console.log('OK')"</automated>
  </verify>
  <done>
    Navbar renders product-appropriate brand name, docs link, and GitHub URL based on active ProductContext. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterize Footer with useProduct</name>
  <files>src/components/Footer.tsx</files>
  <action>
    Edit `src/components/Footer.tsx`:

    1. Add import: `import { useProduct } from "@/context/ProductContext";`

    2. Inside the `Footer` component (at top):
       ```typescript
       const product = useProduct();
       ```

    3. Replace all hardcoded "kdm" references:
       - Brand text `kdm` → `{product.brandName}`
       - Copyright text `© 2026 KDM Labs` → `{product.copyright}`
       - GitHub link `https://github.com/KDM-cli/kdm-cli` → `{product.githubUrl}`

    4. Keep all existing Link components (`/privacy`, `/terms`) as-is — these stay root-level.
    5. Keep all existing CSS classes and layout structure.
    6. `Status` link also uses the GitHub URL — replace that too.
    7. The `Link` import for `@tanstack/react-router` stays for Privacy/Terms.

    Do NOT remove the Link imports — Privacy and Terms still use them.
    Do NOT change the footer layout, spacing, or CSS classes.
  </action>
  <verify>
    <automated>cd D:\Desktop\Yuvraj\kdm-website && node -e "const c=require('fs').readFileSync('src/components/Footer.tsx','utf8'); if(!c.includes('useProduct')){process.exit(1)}; if(!c.includes('product.brandName')){process.exit(2)}; if(!c.includes('product.copyright')){process.exit(3)}; if(!c.includes('product.githubUrl')){process.exit(4)}; console.log('OK')"</automated>
  </verify>
  <done>
    Footer renders product-appropriate brand, copyright, and GitHub link. Build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| n/a | No user input — reads from React context set by route hierarchy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | nav links | accept | Links are static config values, not user-injectable |
</threat_model>

<verification>
1. Navigate to `/kdm` — Navbar shows "kdm", Footer shows "kdm / © 2026 KDM Labs"
2. Navigate to `/docker-guard` — Navbar shows "dg", Footer shows "dg / © 2026 KDM Labs"
3. All links (Features, Commands, Docs, Privacy, Terms, GitHub) work
4. `npm run build` succeeds
</verification>

<success_criteria>
- Navbar brand name, docs link, GitHub URL all come from ProductContext
- Footer brand name, copyright, GitHub link all come from ProductContext
- Privacy/Terms links remain at root level
- Build compiles cleanly
</success_criteria>
